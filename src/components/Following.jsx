import styled from 'styled-components';

const Following = () => {
  return <Following_wrapper>팔로잉</Following_wrapper>;
};

const Following_wrapper = styled.div`
  height: 600px;
  border: 1px black solid;
  padding: 10px;
  position: sticky;
  top: 90px;
`;
export default Following;
